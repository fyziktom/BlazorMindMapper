
<PanzoomMermaid @ref="mermaidViewRef" />

@code {

    PanzoomMermaid? mermaidViewRef;

    public async Task LoadMermaid(string mermaid)
    {
        if (mermaidViewRef != null && mermaid != null)
            await mermaidViewRef.LoadText(mermaid);
    }
}
